import { Helmet } from "react-helmet-async";

const Community = () => {
    return (
        <div>
            <Helmet>
                <title>GYMSTER || Community</title>
            </Helmet>
        </div>
    );
};

export default Community;