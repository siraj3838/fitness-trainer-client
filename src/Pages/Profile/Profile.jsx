import { Helmet } from "react-helmet-async";

const Profile = () => {
    return (
        <div>
            <Helmet>
                <title>GYMSTER || Profile</title>
            </Helmet>
        </div>
    );
};

export default Profile;